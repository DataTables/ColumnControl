<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="8" table-class="display nowrap">

<css lib="datatables columncontrol" />
<js lib="datatables columncontrol">
<![CDATA[

new DataTable('#example', {
	columnControl: ['order', ['search', 'searchList']],
	ordering: {
		indicators: false,
		handler: false
	}
});
]]>
</js>

<title lib="ColumnControl">Both list and input search</title>

<info><![CDATA[

The other search examples have all shown `-content searchList` and `-content search` independently but it is quite possible to use them together in ColumnControl.

This example demonstrates that and allows the end user to use the search input type that they are most comfortable with and makes the most sense for the data in question.

]]></info>

</dt-example>
